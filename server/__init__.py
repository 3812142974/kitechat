"""KiteChat server package.

OneBot V11 compatible private AI chat system:
- server core (auth, sessions, friends, message relay, OneBot V11 bridge)
- admin WebUI (WS address, SMTP config, one-click client export)
- HTTP/WS single-port entry (aiohttp)
"""

__version__ = "1.0.0"
